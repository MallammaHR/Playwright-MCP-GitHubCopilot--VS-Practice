# Playwright OpenCart Login Tests

Contains a `LoginPage` page object and two tests (positive and negative) targeting OpenCart demo site.

Run tests:

```bash
npm install
npx playwright install
npm test
```
